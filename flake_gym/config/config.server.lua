-- ============================================================
--  flake_gym  –  config.server.lua  (server-side only config)
-- ============================================================

-- This file is loaded only on the server side.
-- Most configuration is in config.lua and config.management.lua.
-- Add any server-specific overrides here if needed.

-- Example: override the database table names if your setup uses different ones
-- Config.DBTableUsers   = "users"   -- ESX: "users",  QB-Core: "players"
-- Config.DBTablePlayers = "players"
